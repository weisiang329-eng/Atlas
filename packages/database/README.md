# Atlas Database Package

Prisma schema skeleton for the Atlas PostgreSQL database.

## Commands

```bash
npm run typecheck --workspace @atlas/database
```

The initial schema intentionally models only foundational company identity data. Product scoring, ingestion, and AI runtime models should be added in later sprints after specification review.
