# Handoff — 2026-07-21 (backend foundation + live wiring + glove merge)

**Read this first if you are picking up Atlas cold.** It says exactly what
exists, what is in flight, and the next command to run. Update it in every PR.

## Owner directives in force

1. Drive the whole roadmap dependency-first (owner said "全部" — everything).
2. **Everything deploys to Cloudflare** (Workers + D1 + Pages). No other infra.
3. Every milestone = plan doc in repo + PR on GitHub, so any agent/person can
   take over at any time. Never leave work only in a local tree or chat.

## PR stack (in order)

| PR | Branch | Contents | State |
| --- | --- | --- | --- |
| PR 0 | `docs/management-plans` | This doc + program docs P004/P026 + wiring plan + roadmap update | this PR |
| PR 1 | `feat/backend-foundation` | `apps/api` (Hono+D1+Drizzle, engine, seed, verified), root monorepo files, CI | open next |
| PR 2 | `feat/web-live-wiring` | Client data layer + rewire financials/companies pages to live API | in progress — see `management/plans/web-live-wiring-plan.md` §Remaining |
| PR 3 | `feat/glove-sector` | Glove industry + companies + quarterly facts from glove-tracker real data | planned — see `management/programs/P026-glove-sector-merge.md` Phase 1 |

Merge order: 1 → 2 → 3 (each stacks on the previous branch). PR #4 (Codex
Node+Prisma scaffold) is **superseded by PR 1** — recommend closing it.

## What is done and verified (local)

Backend: schema (`source`/`industry`/`company`/`financial_period`/
`financial_fact`), concept catalog + statement specs, ratio engine,
presenters, Hono routes, seed (10 AI-infra companies, 40 periods, 463 facts).
Verified end-to-end with `wrangler dev` + local D1: NVDA FY25 margins match
reported figures; sparse/loss-year/split-adjusted cases correct; typecheck
green. Details + verify commands: `management/programs/P004-backend-foundation.md`.

Web data layer written (not yet wired into pages): `lib/types.ts`,
`lib/universe.ts`, `lib/loaders/use-api.ts`,
`components/financial/subject-context.tsx`, `subject-selector.tsx`.

## How to run locally

```
npm install                      # repo root (workspaces)
cd apps/api
npx wrangler d1 migrations apply atlas-db --local
node seed/generate.mjs && npx wrangler d1 execute atlas-db --local --file=seed/seed.sql
npx wrangler dev --port 8787     # API at http://127.0.0.1:8787
# smoke: curl http://127.0.0.1:8787/v1/companies/nvidia/financials
cd ../web && NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8787 npm run dev
```

## Owner actions required (blocking deploy only)

```
cd apps/api
npx wrangler d1 create atlas-db          # paste database_id into wrangler.toml
npx wrangler d1 migrations apply atlas-db --remote
npx wrangler d1 execute atlas-db --remote --file=seed/seed.sql
npx wrangler deploy
# then: Pages → env NEXT_PUBLIC_API_BASE_URL=<worker URL> → redeploy web
```

## Next work queue (after PR 3)

1. Quarterly facts for AI-infra names (results pages fully live).
2. P005 Company Intelligence (profile/products/management from real sources).
3. P026 Phase 2 (commodities + industry metrics + gloves industry workspace).
4. P022 ingestion spike: SEC EDGAR companyfacts → `financial_fact` (concepts
   already XBRL-shaped).
5. P010 scoring: `score_model`/`company_score` per `schemas/database-v0.md`.

## Conventions

- Programs large enough for multiple PRs; every program has a doc in
  `management/programs/`. Plans in `management/plans/`. Reports in
  `management/reports/`.
- API responses mirror `apps/web/lib/types.ts` — change presenters and types
  together, never one side.
- Facts: positive magnitudes; sign is presentation. Missing data renders `—`,
  never fabricated. Every value traces to a `source` row.
