# Cloudflare Pages redirects (static export drops Next.js runtime redirects()).
# Legacy routes kept alive from the original prototype:
/dashboard              /              302
/company                /companies     302
# Bare company route → its overview tab:
/companies/:companyId   /companies/:companyId/overview   302
